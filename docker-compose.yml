version: '3.8'

services:
  websocket-server1:
    image: centos:7
    command: bash -c 'yum install -y git java-1.8.0-openjdk && git clone https://github.com/hahmjt/cmpt471_project.git ~/project && ls ~/project/'
